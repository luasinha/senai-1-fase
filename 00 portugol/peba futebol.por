programa {
  funcao inicio() {
    inteiro vitoria, empate, pontos
    escreva("numero de vitorias: ")
    leia(vitoria)
    escreva("numero de empates: ")
    leia(empate)
    inteiro total = (vitoria*3) + empate
    escreva("total de pontos: " + total)
  }
}
